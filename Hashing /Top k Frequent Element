/* 
LeetCode 347 : Top k frequent element.

Apporach : 
store frequency of every element using map or unordered map.
Than use priority uqeue (max heap ) to store the value with frequency 
and than store k element in result vector to return result.

Time Complexity : O(n log n )
Space Complexity : O(n)
*/

class Solution {
public:
    vector<int> topKFrequent(vector<int>& nums, int k) {
        unordered_map<int , int> mp;
        priority_queue<pair<int, int>> pq;
        for(auto val : nums){
            mp[val]++;
        }
        for(auto &it : mp){
            pq.push({it.second, it.first});
        }
        vector<int> result;
        for(int i = 0; i < k; i++){
            result.push_back(pq.top().second);
            pq.pop();
        }
        return result;
    }
};
